version: "3.9"
services:
  frontend:
    build: ./frontend
    restart: always
  backend:
    build: ./backend
    restart: always
    volumes:
      - /var/log/sso/:/var/log/sso/
    depends_on:
      - frontend
      - surrealdb
  surrealdb:
    image: surrealdb/surrealdb:latest
    command: start --log debug --user root --pass root rocksdb:/data/db
    volumes:
      - surrealdb-data:/data/db
    restart: unless-stopped
    user: "1000:1000"

volumes:
  surrealdb-data: